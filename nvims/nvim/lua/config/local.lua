-- Personal settings live here so the rest of the configuration stays easy to
-- update and reason about.
return {
  -- Use "glyph" when the terminal is configured with a Nerd Font. "ascii" is
  -- the portable default and avoids broken icon boxes.
  icon_style = "ascii",

  workbench = {
    -- default_layout = "split", -- "split", "float", or "tab"
    -- split_height = 18,
    -- profiles = {
    --   codex = {
    --     command = "codex",
    --     attention_patterns = {
    --       "Press Enter to continue",
    --       "Allow this",
    --     },
    --   },
    -- },
  },
}
